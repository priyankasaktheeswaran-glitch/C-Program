#include <stdio.h>
int main() {
    int units=120;
    int bill;
    if(units<=100)
        bill = units*5;
    else
        bill = 100*5 + (units-100)*10;
    printf("Bill amount = Rs. %d", bill);
    return 0;
}
